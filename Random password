import random
import string 
def get_password():
length = int (8)
password_character = string.ascii_letters + string.digits + string.panctuation
password = ''.join(random.choice (password_character) for i in range (length))
print (password)
