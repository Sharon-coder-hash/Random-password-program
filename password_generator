import random
import string
def get_password(length):
    random_password = string.ascii_letters + string.digits + string.punctuation
    pasword_character = ''.join(random.choice(random_password) for i in range(length))
    print(password_character)
    
    get_password(8)
